.friends {
    .warn {
        display: inline-flex;
        flex-direction: row;
        i {
            margin-top: 7px;
            margin-right: 10px;
        }
        #animate {
            animation: iconAnimate 1.33s ease-in-out infinite;
        }
    }

    .content {
        ul li {
            display: inline;
        }
    }
}